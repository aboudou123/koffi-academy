Koffi Academy local Docker lab
================================

This is an isolated Ubuntu workspace for hands-on exercises.

Useful commands:

  pwd
  ls -la
  whoami
  ip addr
  curl --version
  git --version
  python3 --version
  jq --version

Workspace:

  /home/dev/workspace

The container is disposable. Files disappear when the lab session is stopped.
